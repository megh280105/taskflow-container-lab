{{- range $component := list "backend" "worker" "frontend" "postgres" "redis" }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "taskflow.serviceAccountName" (list $ $component) }}
  labels:
    {{- include "taskflow.labels" $ | nindent 4 }}
automountServiceAccountToken: false
---
{{- end }}
